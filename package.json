{
  "name": "zipify",
  "version": "1.0.0",
  "description": "CLI tool to generate Fastify routes, controllers, and services",
  "bin": {
    "zipify": "./build/index.js"
  },
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
"scripts": {
  "build": "tsc",
  "postbuild": "cp -R templates build/",
  "start": "npm run build && npm run postbuild && cd build && npm link && cd .. && zipify"
},
  "dependencies": {
    "commander": "^14.0.0",
    "inquirer": "^12.9.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.5.4"
  },
  "license": "MIT",
  "private": true
}